<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.path.dao.PathDao">

    <!-- 分页查询 -->
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.path.domain.vo.PathVO">
        SELECT
        *,(select actual_name from t_employee where t_employee.employee_id=t_path.chargeperson)chargeperson_name
        FROM t_path
        <where>
            <!--路线编码/名称-->
            <if test="queryForm.code != null and queryForm.code != ''">
                AND code like concat('%', #{queryForm.code}, '%')
            </if>

            <if test="queryForm.name != null and queryForm.name != ''">
                AND name like concat('%', #{queryForm.name}, '%')
            </if>

            <if test="queryForm.isDisabled != null">
                AND is_disabled = #{queryForm.isDisabled}
            </if>
        </where>
    </select>

    <select id="queryPath" resultType="net.lab1024.sa.admin.module.business.path.domain.vo.PathVO">
        SELECT id, id pathId, code, name FROM t_path
        WHERE 1=1 and is_disabled=0
        <if test="queryForm.code != null and queryForm.code != ''">
            AND (code = #{queryForm.code}
            OR name LIKE concat('%', #{queryForm.code}, '%'))
        </if>
        <if test="queryForm.name != null and queryForm.name != ''">
            AND name LIKE concat('%', #{queryForm.name}, '%')
        </if>
    </select>

    <insert id="insertPath">
        INSERT INTO t_path(code, name, is_disabled, chargeperson, ctime, cemp_name, description, ts01)
        VALUES ( #{code}, #{name}, #{isDisabled}, #{chargePerson}, #{ctime}, #{cempName}, #{description}, #{ts01})
    </insert>


    <update id="updatePath">
        UPDATE t_path
        SET code        = #{code},
            name        = #{name},
            is_disabled = #{isDisabled},
            chargeperson= #{chargePerson},
            utime       = #{utime},
            uemp_name   = #{uempName},
            description = #{description},
            ts01        = #{new_ts01}
        WHERE id = #{id} and ts01= #{ts01}
    </update>



</mapper>